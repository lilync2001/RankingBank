import { environment } from 'src/environments/environment';

export default {
  URL_BASE: environment.url,
  URL_BASE_PATH: environment.baseUrl,
  URL_API_BASE: environment.urlApi,
  URL_API_SOCKET: environment.urlApiSocket,
};
